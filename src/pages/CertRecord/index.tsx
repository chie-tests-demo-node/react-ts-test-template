import { useCallback, useEffect, useState } from 'react'



const CertRecord: React.FC = () => {
  const [loading, setLoading] = useState<boolean>(false)
  useCallback(() => { }, [])
  useEffect(() => { })

  return (
    <>
      这是证书记录
    </>
  )
}

export default CertRecord